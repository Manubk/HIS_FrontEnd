import { UniqueSearchFilters } from './unique-search-filters';

describe('UniqueSearchFilters', () => {
  it('should create an instance', () => {
    expect(new UniqueSearchFilters()).toBeTruthy();
  });
});
